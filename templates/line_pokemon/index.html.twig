{% extends 'base.html.twig' %}

{% block title %}LinePokemon index{% endblock %}

{% block body %}
    <h1>LinePokemon index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for line_pokemon in line_pokemons %}
            <tr>
                <td>{{ line_pokemon.id }}</td>
                <td>{{ line_pokemon.name }}</td>
                <td>
                    <a href="{{ path('app_line_pokemon_show', {'id': line_pokemon.id}) }}">show</a>
                    <a href="{{ path('app_line_pokemon_edit', {'id': line_pokemon.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_line_pokemon_new') }}">Create new</a>
{% endblock %}
