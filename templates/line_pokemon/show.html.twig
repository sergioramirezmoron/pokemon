{% extends 'base.html.twig' %}

{% block title %}LinePokemon{% endblock %}

{% block body %}
    <h1>LinePokemon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ line_pokemon.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ line_pokemon.name }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ line_pokemon.level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_line_pokemon_index') }}">back to list</a>

    <a href="{{ path('app_line_pokemon_edit', {'id': line_pokemon.id}) }}">edit</a>

    {{ include('line_pokemon/_delete_form.html.twig') }}
{% endblock %}
